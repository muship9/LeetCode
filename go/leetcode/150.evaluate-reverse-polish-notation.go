package leetcode

func twoSum(numbers []int, target int) []int {
	l := 0            // 左側のポインタ
	r := len(numbers) // 右側のポインタ
	var result []int

	print(l)
	print(r)
	print(result)

	// TODO : ポインタをずらしていって配列の値をチェックしたい
	for i := 0; i < len(numbers); i++ {
	}
	return result
}
